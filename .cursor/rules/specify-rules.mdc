# speckit-bedrock-demo Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-02-26

## Active Technologies
- Amazon S3 (document objects), Amazon S3 Vectors (embeddings / RAG — native vector storage and query in S3; Bedrock integration; cost-optimized); metadata for document list and processing status in DynamoDB or in-app store aligned with S3/vector lifecycle (001-document-rag-api)
- Amazon S3 (document objects; SSE-S3 or AWS default encryption at rest), Amazon S3 Vectors (embeddings / RAG); metadata for document list and processing status in DynamoDB or in-app store aligned with S3/vector lifecycle (001-document-rag-api)

- Python 3.12 + FastAPI (API), boto3 (AWS SDK), PyPDF2 or pypdf + markdown (text extraction), opentelemetry-api/sdk/exporter-otlp (observability), structlog or equivalent (structured logs) (001-document-rag-api)

## Project Structure

```text
backend/
frontend/
tests/
```

## Commands

cd src [ONLY COMMANDS FOR ACTIVE TECHNOLOGIES][ONLY COMMANDS FOR ACTIVE TECHNOLOGIES] pytest [ONLY COMMANDS FOR ACTIVE TECHNOLOGIES][ONLY COMMANDS FOR ACTIVE TECHNOLOGIES] ruff check .

## Code Style

Python 3.12: Follow standard conventions

## Recent Changes
- 001-document-rag-api: Added Python 3.12 + FastAPI (API), boto3 (AWS SDK), PyPDF2 or pypdf + markdown (text extraction), opentelemetry-api/sdk/exporter-otlp (observability), structlog or equivalent (structured logs)
- 001-document-rag-api: Added Python 3.12 + FastAPI (API), boto3 (AWS SDK), PyPDF2 or pypdf + markdown (text extraction), opentelemetry-api/sdk/exporter-otlp (observability), structlog or equivalent (structured logs)

- 001-document-rag-api: Added Python 3.12 + FastAPI (API), boto3 (AWS SDK), PyPDF2 or pypdf + markdown (text extraction), opentelemetry-api/sdk/exporter-otlp (observability), structlog or equivalent (structured logs)

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
